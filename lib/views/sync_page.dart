import 'package:flutter/material.dart';

class SyncOnline extends StatelessWidget {
  const SyncOnline({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
